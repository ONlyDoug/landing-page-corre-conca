# Interface

## Componente Kit
Foi adicionada uma div inferior (`data-testid="info-section"`) com as informações de retirada, usando um layout responsivo (`flex-col` para mobile, `flex-row` para desktop) e acessibilidade configurada via `role="region"` e `aria-label="Retirada de Kits"`.
