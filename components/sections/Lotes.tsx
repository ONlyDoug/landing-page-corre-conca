import { VALOR_INSCRICAO, CONDICAO_INSCRICAO } from '@/lib/constants'
import { formatBRL } from '@/lib/utils'

export function Lotes() {
  // A seção de vendas foi ocultada temporariamente (SPEC-0002)
  return null
}
